-> It is the service that allows developers to easily create a backend with a postgres database.
-> Automatically creates a database and API to send and receive data from the server.
-> No back -end development needed
-> It also comes with easy to use user authentication and file storage.
