/*
What Is the Same-Origin Policy in JavaScript?

Same origin policy is a security ,esaure that prevents the web page hoisted though one origin to interact with the resources from another origin.

the main purpose if to prevent the malicious script from accessing your data.
If you want to Bypass SOP,
1. Use CORS (Cross-Origin Resource Sharing)
2. Use JSONP(Json with padding)
3. postMessage API for safe cross-window or cross-iframe communication


 */
